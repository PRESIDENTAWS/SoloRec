import { Briefcase } from "lucide-react";
import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function JobsPage() {
  return (
    <PlaceholderPage
      icon={Briefcase}
      title="Jobs"
      description="Job requisitions, structured intake, and pipeline status will live here — see Epic 3 in docs/product/backlog.md."
    />
  );
}
