import { Building2 } from "lucide-react";
import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function CompaniesPage() {
  return (
    <PlaceholderPage
      icon={Building2}
      title="Companies"
      description="Company/account records will live here, alongside Clients — see Epic 2 in docs/product/backlog.md."
    />
  );
}
