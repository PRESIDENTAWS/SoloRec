import { Users } from "lucide-react";
import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function CandidatesPage() {
  return (
    <PlaceholderPage
      icon={Users}
      title="Candidates"
      description="Candidate profiles, documents, and structured/semantic search will live here — see Epic 4 in docs/product/backlog.md."
    />
  );
}
