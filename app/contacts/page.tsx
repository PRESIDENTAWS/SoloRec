import { Contact } from "lucide-react";
import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function ContactsPage() {
  return (
    <PlaceholderPage
      icon={Contact}
      title="Contacts"
      description="Client-side contacts will live here — see Epic 2 in docs/product/backlog.md."
    />
  );
}
